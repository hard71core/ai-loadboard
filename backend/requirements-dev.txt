# Tooling for local development and CI — not shipped in the Docker image.
-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0   # required by fastapi.testclient.TestClient
ruff>=0.6,<1.0
