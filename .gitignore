node_modules/
dist/
__pycache__/
*.pyc
.venv/
.env
.idea/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage/

# TS project-reference build output for vite.config.ts/vitest.config.ts —
# tsc emits these next to the source because `composite: true` requires
# *some* emit; they're pure build cache, not source.
frontend/vite.config.js
frontend/vite.config.d.ts
frontend/vitest.config.js
frontend/vitest.config.d.ts
*.tsbuildinfo

# Claude Code local session state — not shared project config.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local done/to-do tracking note — source of truth is docs/technical-
# documentation.html §17 and .claude/rules/security.md, this is just a
# convenience summary, not meant to be committed.
.claude/PROGRESS.local.md
